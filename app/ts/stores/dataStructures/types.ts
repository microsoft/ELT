// Typedefs.

export type TabID = 'file' | 'alignment' | 'labeling' | 'build' | 'options';

export enum KeyCode {
    BACKSPACE = 8,
    TAB = 9,
    LINEFEED = 10,
    ENTER = 13,
    ESCAPE = 27,
    SPACE = 32,
    PAGE_UP = 33,
    PAGE_DOWN = 34,
    END = 35,
    HOME = 36,
    LEFT = 37,
    UP = 38,
    RIGHT = 39,
    DOWN = 40,
    DELETE = 46,
    NUMPAD_MULTIPLY = 106,
    NUMPAD_ADD = 107,
    NUMPAD_ENTER = 108,
    NUMPAD_SUBTRACT = 109,
    NUMPAD_DECIMAL = 110,
    NUMPAD_DIVIDE = 111,
    COMMA = 188,
    PERIOD = 190
}
